#include <stdio.h>
#include <stdlib.h>
#include <string.h>
int Rec(struct tree *, char, int);
struct tree
{
	int Val;
	struct tree *First, *Second;
};
void main()
{

	getchar();
	return 0;
}
int Rec(struct tree *Cur, char str, int length)
{
	if (length > 1)
	{
		if ()
	}
}